import React from 'react';
import Table from '../table';

const App = () => (
  <div>
    <header>
      <h1>Sortable React/Redux Table</h1>
    </header>

    <main>
      <Table />
    </main>
  </div>
);

export default App;
